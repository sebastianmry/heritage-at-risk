# Pipeline runtime (stages 1 to 3: ingest, processing, export).
# Pinned to exact versions for reproducibility.
#
# These versions match the "heritage" conda env (as of 2026-06-12). The
# GDAL-bound packages come via conda-forge, quackosm via pip.
# environment.yml is the authoritative reproduction state.

duckdb==1.5.3
quackosm==0.17.1
geopandas==1.1.3
shapely==2.1.2
pyproj==3.7.2
pyarrow==24.0.0
pandas==3.0.3
requests==2.34.2
lxml==6.1.1

# Install leafmap (exploration/notebook) separately if needed, not part of the pipeline runtime.
